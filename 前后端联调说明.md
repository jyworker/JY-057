# 拥抱妈妈 · 前后端联调说明

## 📋 概述

本文档说明前后端联调的关键点和测试步骤，确保4个核心功能正常运作。

---

## ✅ 已完成的功能模块

### 1. 首页改造
- ✅ 添加了4个核心功能入口卡片（虚拟拥抱、报平安、纪念空间、爱的回响）
- ✅ 添加了始终可见的"添加成员"按钮
- ✅ 优化了家庭成员列表展示
- ✅ 改进了空状态提示

**文件修改:**
- `front/pages/index/index.wxml`
- `front/pages/index/index.ts`
- `front/pages/index/index.wxss`

### 2. 爱的回响主页（新增）
**页面路径:** `pages/echo/index`

**功能模块:**
- 📊 年度报告生成与查看
- 💌 妈妈回复列表（支持语音/文字/表情）
- 🌱 公益联动数据展示
- 📚 历史报告入口

**关键功能:**
- 自动加载最新报告
- 显示未读回复数量
- 实时公益统计数据
- 支持下拉刷新

**文件列表:**
- `front/pages/echo/index.wxml`
- `front/pages/echo/index.ts`
- `front/pages/echo/index.wxss`
- `front/pages/echo/index.json`

### 3. 年度报告详情页（新增）
**页面路径:** `pages/echo-report/index`

**功能特性:**
- 精美的报告封面设计
- 数据摘要卡片展示
- 最活跃月份高亮
- 温暖留言展示
- 公益贡献可视化
- 支持分享到微信/朋友圈

**文件列表:**
- `front/pages/echo-report/index.wxml`
- `front/pages/echo-report/index.ts`
- `front/pages/echo-report/index.wxss`
- `front/pages/echo-report/index.json`

### 4. 公益联动详情页（新增）
**页面路径:** `pages/echo-welfare/index`

**功能特性:**
- 平台公益数据统计
- 个人贡献排名
- 里程碑展示
- 公益故事墙（支持分页加载）
- AI向善理念展示

**文件列表:**
- `front/pages/echo-welfare/index.wxml`
- `front/pages/echo-welfare/index.ts`
- `front/pages/echo-welfare/index.wxss`
- `front/pages/echo-welfare/index.json`

---

## 🔌 前后端接口对照

### 爱的回响接口
| 前端调用 | 后端接口 | 说明 |
|---------|---------|------|
| `generateAnnualLoveReport(year)` | `POST /api/echo/annual-report` | 生成年度报告 |
| `getAnnualReports()` | `GET /api/echo/annual-reports` | 获取报告列表 |
| `getAnnualReportDetail(reportId)` | `GET /api/echo/annual-report/{reportId}` | 获取报告详情 |
| `shareAnnualReport(reportId)` | `POST /api/echo/annual-report/{reportId}/share` | 分享报告 |
| `getMotherReplies(memberId)` | `GET /api/echo/replies/{memberId}` | 获取妈妈回复 |
| `markReplyAsRead(replyId)` | `PUT /api/echo/reply/{replyId}/read` | 标记已读 |
| `getPublicWelfareStats()` | `GET /api/echo/public-welfare/stats` | 获取公益统计 |
| `getMyWelfareContribution()` | `GET /api/echo/public-welfare/my-contribution` | 获取个人贡献 |
| `getWelfareStories(page, pageSize)` | `GET /api/echo/public-welfare/stories` | 获取公益故事 |

---

## 🧪 测试步骤

### 1. 启动后端服务
```bash
cd back
./mvnw spring-boot:run
```

确认后端服务运行在 `http://localhost:8080`

### 2. 启动前端小程序
1. 打开微信开发者工具
2. 导入项目：选择 `front` 目录
3. 配置本地服务器域名（开发环境）
4. 点击编译运行

### 3. 测试首页功能
- [ ] 首页显示4个核心功能卡片
- [ ] 点击"添加成员"按钮可正常跳转
- [ ] 家庭成员列表正常加载
- [ ] 点击各功能卡片可正常跳转

### 4. 测试爱的回响主页
- [ ] 访问 `/pages/echo/index` 页面
- [ ] 年度报告卡片正常显示
- [ ] 点击"生成报告"按钮功能正常
- [ ] 妈妈回复列表正常加载
- [ ] 公益数据正常展示
- [ ] 下拉刷新功能正常

### 5. 测试年度报告详情
- [ ] 从爱的回响页面点击"查看报告"
- [ ] 报告封面正常显示
- [ ] 数据摘要卡片正常显示
- [ ] 点击"分享报告"按钮功能正常
- [ ] 支持转发到微信好友/朋友圈

### 6. 测试公益联动详情
- [ ] 从爱的回响页面点击"查看详情"
- [ ] 平台统计数据正常显示
- [ ] 个人贡献数据正常显示
- [ ] 公益故事列表正常加载
- [ ] 点击"加载更多"功能正常

---

## 🐛 常见问题排查

### 1. 接口调用失败
**症状:** Toast提示"加载失败"或请求超时

**排查步骤:**
1. 检查后端服务是否正常运行
2. 查看浏览器/小程序控制台的网络请求
3. 确认 `front/utils/request.ts` 中的 baseURL 配置正确
4. 检查 Token 是否有效（查看 TokenInterceptor 日志）

**示例修复:**
```typescript
// front/utils/request.ts
const baseURL = 'http://localhost:8080'  // 开发环境
```

### 2. 数据显示不正确
**症状:** 页面显示空数据或字段为 undefined

**排查步骤:**
1. 检查后端返回的数据结构
2. 对比前端类型定义 `front/types/index.d.ts`
3. 查看数据库中是否有测试数据
4. 检查数据格式化逻辑（如时间格式）

**示例修复:**
```typescript
// 时间格式化
if (report.generateTime) {
  const date = new Date(report.generateTime)
  report.generateTime = `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}`
}
```

### 3. 页面跳转404
**症状:** 点击功能卡片后显示"页面不存在"

**排查步骤:**
1. 确认 `front/app.json` 中已注册新页面
2. 检查跳转路径是否正确
3. 确认页面文件是否存在

**已注册页面:**
```json
{
  "pages": [
    "pages/index/index",
    "pages/echo/index",
    "pages/echo-report/index",
    "pages/echo-welfare/index"
  ]
}
```

### 4. 样式显示异常
**症状:** 页面布局混乱或样式丢失

**排查步骤:**
1. 检查 wxss 文件是否正确加载
2. 确认 Vant Weapp 组件库已正确引入
3. 查看微信开发者工具的样式警告

---

## 📊 数据库测试数据

### 插入测试数据
```sql
-- 插入测试用户
INSERT INTO hm_user (user_id, openid, nick_name, avatar_url) 
VALUES ('user_test_001', 'openid_test_001', '测试用户', 'https://example.com/avatar.jpg');

-- 插入测试家庭成员
INSERT INTO hm_family_member (member_id, user_id, name, nickname, status, photo_hash) 
VALUES ('member_001', 'user_test_001', '王芳', '妈妈', 'alive', 'hash_001');

-- 插入测试拥抱记录
INSERT INTO hm_hug_record (hug_id, user_id, member_id, duration, timestamp) 
VALUES ('hug_001', 'user_test_001', 'member_001', 3000, NOW());

-- 插入测试年度报告
INSERT INTO hm_annual_report (
  report_id, user_id, year, total_hugs, total_safety_messages, 
  total_distance, warm_days, most_active_month, favorite_message, generate_time
) VALUES (
  'report_2026_001', 'user_test_001', 2026, 36, 48, 2870.5, 
  365, '2月', '妈妈，我爱你！', NOW()
);

-- 插入测试公益统计
INSERT INTO hm_welfare_platform_stats (
  stat_date, total_platform_hugs, total_welfare_value, 
  helped_families_count, current_month_hugs, realtime_contributors
) VALUES (
  CURDATE(), 10000, 100.00, 10, 500, 20
);
```

---

## 🚀 部署建议

### 前端部署
1. 修改 `front/utils/request.ts` 中的 baseURL 为生产环境地址
2. 在微信公众平台配置服务器域名白名单
3. 上传代码到微信小程序后台
4. 提交审核

### 后端部署
1. 修改 `back/src/main/resources/application.yml` 配置
2. 打包项目：`./mvnw clean package`
3. 部署 jar 包到服务器
4. 配置数据库连接
5. 启动服务

---

## 📞 技术支持

如有问题，请查阅以下文档：
- 项目书：`项目书/功能书.md` 和 `项目书/技术栈.md`
- 接口文档：`爱的回响-前后端接口对照表.md`
- 功能说明：`back/爱的回响功能说明.md`

**更新日期:** 2026-02-04  
**版本:** v1.0.0
